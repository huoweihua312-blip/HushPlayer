# Retained as a migration note; this file is no longer passed to PyInstaller.
# packaging/prepare_version_metadata.py now generates the real Windows version
# resource from app/core/version.py into the selected build output directory.
